---
collections:
  - name: amazon.aws
    version: ">=9.0.0"
  - name: azure.azcollection
    # >=3.20.0 required: 3.17.0 added native OIDC / workload-identity federation to the azure_rm
    # inventory plugin (oidc_token_file_path / AZURE_FEDERATED_TOKEN_FILE), replacing the archived
    # oidc-ansible-inventory wrapper; 3.20.0 added the AZURE_TENANT_ID fallback and caller
    # subscription_id merge on that auth path (ansible-collections/azure#2274). Do not lower.
    version: ">=3.20.0"
  - name: google.cloud
    version: ">=1.4.0"
  - name: community.vmware
    version: ">=5.0.0"
  - name: community.general
    version: ">=10.0.0"
  - name: ansible.posix
    version: ">=2.0.0"
  - name: ansible.netcommon
    version: ">=7.0.0"
