# Copyright (c) 2025 VH & Co BV. Licensed under the Apache License 2.0. See LICENSE for details.

name: Release

on:
  push:
    tags:
      - '*.*.*'

permissions:
  contents: write
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Build and push
        uses: michielvha/docker-release-action@main
        with:
          version: ${{ github.ref_name }}
          registry: ghcr.io
          username: vhco-pro
          password: ${{ secrets.GITHUB_TOKEN }}
          project: stackweaver-ansible-runner
          platforms: linux/amd64,linux/arm64
          context: .
          cache-from: type=registry,ref=ghcr.io/vhco-pro/stackweaver-ansible-runner:buildcache
          cache-to: type=registry,ref=ghcr.io/vhco-pro/stackweaver-ansible-runner:buildcache,mode=max
          trivy-exit-code: '0'

      - name: Changelog & GitHub Release
        uses: michielvha/changelog-release-action@main
        with:
          version: ${{ github.ref_name }}
